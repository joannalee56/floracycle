// 'use strict';

// function initMap() {
  
//   const postalCode = document.querySelector('.postal_code_to_name').innerText;
//   const geocoder = new google.maps.Geocoder();
//   geocoder.geocode({address: postalCode}, (results, status) => {
//       if (status === 'OK') {
//       // Get the coordinates of the user's location
//       const cityState = results[0].formatted_address;
      
//       document.querySelector('#city-state-zip').innerText = cityState
      
//       } else {
//       alert(`Geocode was unsuccessful for the following reason: ${status}`);
//       }
//   });
// }



